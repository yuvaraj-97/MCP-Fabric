# HTTP/SSE Transport

HTTP/SSE or Streamable HTTP adapter implementation lives here.

The adapter should expose session identity to the routing layer and keep stream
mechanics out of application logic.
