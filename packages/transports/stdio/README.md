# stdio Transport

stdio adapter implementation and examples live here.

The adapter should connect MCP-compatible protocol messages to process stdin and
stdout while keeping application handlers transport-neutral.
