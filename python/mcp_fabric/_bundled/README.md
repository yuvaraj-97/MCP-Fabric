# Bundled Runtime

Built wheels include the selected JavaScript gateway runtime files here. During
source-tree development, `mcp_fabric.paths` falls back to the repository root so
the Python package can start the local gateway without copying the full repo into
this directory.
